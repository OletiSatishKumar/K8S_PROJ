apiVersion: v1
kind: Service
metadata:
  name: flowershop-service
spec:
  selector:
    app: flowershop
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  type: {{ .Values.service.type }}
